# 贡献指南

感谢你对智鉴黄精AI品质检测系统的关注！

## 开发环境设置

1. Fork 并克隆仓库
2. 创建虚拟环境并安装依赖
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```
3. 复制并配置环境变量
```bash
cp .env.example .env
# 编辑 .env，填入你的测试 API Key
```

## 代码规范

- 遵循 PEP 8 Python 代码风格
- 函数和类添加 docstring 说明
- 重要逻辑添加注释（但避免过度注释）
- 提交前运行 `huangjing check` 进行环境检查

## 提交规范

使用清晰的提交信息：
- `feat: 添加新功能描述`
- `fix: 修复问题描述`
- `docs: 文档更新`
- `refactor: 代码重构`
- `test: 测试相关`
- `chore: 构建/工具链相关`

## Pull Request 流程

1. 从 `main` 分支创建特性分支
2. 完成开发并本地测试
3. 推送到你的 Fork
4. 创建 Pull Request，清晰描述改动内容和原因
5. 等待代码审核

## 重要原则

### 三个指标完全独立

任何修改都不得破坏三个评价指标的独立性：
- 根茎完整度只看结构
- 色泽均匀度只看色泽分布
- 霉变风险只看疑似霉变特征

禁止跨指标联动推理。

### 程序端最终判定

程序端必须按照判定规则重新计算综合结论，不得直接信任模型的 `overall` 字段。

### 模型可替换性

保持 `VisionModel` 抽象接口的稳定性，确保未来替换为 ResNet18 时无需修改 UI 和业务规则。

## 安全要求

- 不得在代码中硬编码 API Key
- 不得将 `.env` 提交到 Git
- 日志中不得记录 API Key、用户图片、完整 Prompt
- 输入验证和错误处理必须完善

## 测试建议

1. 使用符合要求的九蒸九晒黄精图片测试
2. 测试边界情况（模糊图片、多根堆叠、生鲜黄精）
3. 验证三个指标的独立性
4. 验证最终判定规则的优先级
5. 测试 API 超时和重试机制

## 问题反馈

发现 Bug 或有功能建议？请在 GitHub Issues 中提交，包含：
- 问题描述
- 复现步骤
- 预期行为
- 实际行为
- 环境信息（操作系统、Python 版本等）

## 许可证

本项目采用 MIT 许可证，贡献的代码将使用相同许可证。
